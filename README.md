# I2C Driver for RGBW LED Controller

## Описание проекта

Драйвер взаимодействия с микросхемой управления RGBW светодиодами через интерфейс I2C микроконтроллеров семейства Cortex-M4

Драйвер обеспечивает следующие возможности:
* первоначальную инициализацию микросхемы управления RGBW светодиодами (все светодиоды отключены, микросхема готова к работе, микросхема всегда включена); 
* возможность управления яркостью по каждому каналу по отдельности; 
* включение и выключение каждого канала по отдельности; 
* включение и выключения всех каналов одновременно; 
* установку цвета светодиодов в градациях RGB (0 - 255 для каждого цвета) с учетом ограничений микросхемы управления при применении единого RGB светодиода для индикации. 
## Документация на микросхему
 * [Datasheet RGBW Led Controller](https://drive.yadro.com/s/YYQTjwe7LB49HcK)

## Исходный код драйвера находится в файлах:
* [src/RGBW_driver_i2c.h](src/RGBW_driver_i2c.h)
* [src/RGBW_driver_i2c.c](src/RGBW_driver_i2c.c)

## Статический анализ кода
Использовалась утилита ```cppcheck```. Чтобы установить эту утилиту, введите в терминал одну из следующих команд: \
   ```brew install cppcheck``` \
   или, если у вас есть root-права (для Ubuntu / Linux Mint / Debian) \
   ```sudo apt install cppcheck``` \
   \
   Установив cppcheck, вы можете протестировать исходный код: \
   ```cppcheck --enable=all --suppress=missingIncludeSystem src/soursefile_name.c``` \
   Также можно проверить сразу все файлы с исходным кодом в директории: \
   ```cppcheck --enable=all --suppress=missingIncludeSystem src/```